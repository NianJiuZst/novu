{
  "terminals": [],
  "install": "git submodule update --init --recursive && pnpm install && pnpm build:v2",
  "start": "cd scripts && node setup-env-files.js",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}
